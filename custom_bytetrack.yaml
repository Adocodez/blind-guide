tracker_type: bytetrack
track_high_thresh: 0.35
track_low_thresh: 0.1
new_track_thresh: 0.4
track_buffer: 150  # Retains track IDs for ~5 seconds (150 frames)
match_thresh: 0.8
fuse_score: True   # Fixes the AttributeError in modern Ultralytics
